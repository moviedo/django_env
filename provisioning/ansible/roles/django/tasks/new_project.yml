---

- name: create new django projects
  command: "django-admin.py startproject {{ item.item.name }} ."
  args:
    chdir: "{{ item.item.path }}"
  when: item.stdout|length == 0
  with_items: results.results
